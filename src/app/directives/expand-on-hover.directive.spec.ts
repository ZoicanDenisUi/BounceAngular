import { ExpandOnHoverDirective } from './expand-on-hover.directive';

describe('ExpandOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
